// the favorites page will map across all of a user's favorited mixes

// If a user has no favorites, the page will display "No facvorited mixes and can go to the explore page, upon clicking"

import React from 'react'

export class Favorites extends React.Component {
    render() {
        return(
            <div>
                <h1>Favorites</h1>
            </div>
        )
    }

}